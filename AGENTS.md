# プロジェクト開発指針と合意事項 (AGENTS.md)

このドキュメントは、本プロジェクト（冒険者ギルド経営シミュレーション）において AI エージェントおよび開発者が遵守すべきアーキテクチャ設計・開発指針・合意事項を記録したものです。作業開始時には必ず本指針を確認してください。

---

## 1. 基本方針・合意事項（2026-09-16 確定）

### ① サーバー依存ゼロ（完全ローカル・オフライン完結）
* 外部サーバーやクラウドDBへの通信・依存は行わない。
* インフラ費用ゼロ、メンテナンスフリー、完全スタンドアロンで恒久的に動作するクライアント完結型アプリケーションとする。

### ② アーキテクチャ：`core/`（ゲームロジック）と `ui/`（描画）の完全分離
* **Rust の標準的プラクティスを徹底**:
  * `core` モジュール: ゲーム状態（`GameState`）、冒険者・クエスト・倉庫・収支などのドメインモデルおよび進行ロジック。**`egui` や `eframe` への依存を一切持たない純粋な Rust コード**とする。
  * `ui` モジュール: `eframe::App` のライフサイクルおよび `egui` による画面描画。`GameState` を受け取って表示し、ユーザー操作を `core` のメソッド呼び出しまたはイベントに変換するだけに徹する。
* **借用チェッカー対策**:
  * UI描画コンテキストとゲーム状態の可変借用が衝突しないよう、状態更新と描画の責務を明確に分ける。

### ③ セーブ機能とデータ保護（対改ざん性）
* **基盤**: `serde` / `serde_json` を全面導入し、状態構造体に `#[derive(Serialize, Deserialize)]` を付与。
* **開発フェーズ**:
  * デバッグ・検証の容易さを優先し、ローカルのプレーンな JSON 形式（例: `save_data.json`）で保存・読み込みを行う。
* **対改ざん性・将来対応**:
  * セーブ/ロード処理は抽象化（インターフェース化）しておく。
  * 将来的に改ざん防止を導入する際も、サーバーに頼らず **ローカル完結の暗号化（AES-256 等）または HMAC 署名付きバイナリ化** を差し込める設計とする。

### ④ テスト容易性（Unit テストの必須化）
* `core` ロジック（日次進行、固定費引き落とし、クエスト成否判定、アイテム売買、セーブ/ロード等）に対して、画面なし環境（DevContainer / CI）でも `cargo test` 一発で高速動作する Unit テストを必ず整備する。

---

## 2. 推奨ディレクトリ構成

```text
src/
├── main.rs              # エントリポイント、DevContainer/X11設定、フォント設定
├── app.rs               # GuildApp（eframe::App実装、タブ遷移、メインレイアウト）
├── core/                # 【GUI非依存】ゲームドメイン・ロジック・テスト
│   ├── mod.rs           # GameState の統括、save/load 実装
│   ├── state.rs         # ゲーム基本状態（Day, Gold, Rank, 等）
│   ├── adventurer.rs    # 冒険者データ・ステータス
│   ├── quest.rs         # クエストデータ・派遣・完了判定
│   ├── inventory.rs     # 倉庫アイテム・売却計算
│   └── finance.rs       # 固定費・人件費・収支台帳
└── ui/                  # 【egui依存】画面描画・コンポーネント
    ├── mod.rs
    ├── components/      # ヘッダー、固定フッター（日誌ログ）など
    └── views/           # 各タブの描画（ギルド情報、クエスト、冒険者、倉庫、職員、台帳）
```

---

## 3. 実装済みステータス（2026-09-17 完了）

* [x] **依存関係の追加**: `serde`, `serde_json` を導入。
* [x] **`core` モジュールの切り出し**:
  * `src/core/` 配下に `adventurer.rs`, `inventory.rs`, `quest.rs`, `finance.rs`, `state.rs`, `mod.rs` を抽出。
  * 完全 GUI 非依存のゲームロジックおよび JSON セーブ/ロード（`save_game`, `load_game`）を実装。
* [x] **Unit テストの整備**:
  * 日付進行・固定費引き落とし、アイテム売却、クエスト派遣/帰還、JSONセーブ/ロード往復の5件のテストを実装、すべてパス。
* [x] **`ui` モジュールおよび `app.rs` の整理**:
  * ヘッダー、フッター、6つの各タブ画面を `src/ui/` に完全分離。
  * `src/main.rs` はエントリポイント（50行程度）にスリム化。

---

## 4. ゲーム要件・コア仕様（2026-09-19 確定）

### ① 時間スケールと加齢・寿命
* **1年 = 365日**（現実準拠）。
* **加齢と能力減衰**: 冒険者は毎年加齢する。40〜45歳を超えると徐々に身体能力の減衰が始まり、プレイヤーは現役続行か引退かの判断を迫られる。
* **老衰・寿命**: 引退した冒険者（教官）も年齢を重ね、寿命（70〜80代前後）を迎えると老衰により永眠（ギルドの殿堂・追悼録に記録）。

### ② 冒険者のリスクと世代交代（戦死・引退・指導員）
* **戦死（キャラロスト）: あり**。高難度クエストでの全滅や、HP0時の救護失敗で死亡する。
* **引退冒険者の教官雇用（訓練所システム）**:
  * 引退した冒険者はギルドの「訓練所（道場）」の教官（指導員・職員）として雇用可能。
  * 老衰で死ぬまで後輩冒険者を指導し、**教官自身の得意分野に応じた特定パラメータの成長率向上ボーナス**を付与できる。

### ③ 契約形態と収支構造
* **冒険者（登録制）**:
  * **維持費（固定給）はゼロ**。
  * ギルドの収入は「クエスト仲介手数料（報酬の一部）」および「持ち帰り素材・アイテムの売却マージン」。
* **職員（給与制）**:
  * 引退教官、受付係、清掃・警備スタッフ等は **契約金（採用時）および維持費（日給/月給）** が発生。

### ④ 発注者（クライアント勢力）と名声・信頼度
* **発注者カテゴリ**:
  1. **国家・公的機関**（王宮・騎士団・領主）: 治安維持、魔物討伐。大量の**名声値**とランク昇格推薦。
  2. **商会・企業**（商業ギルド・鉱山・工房）: 資源採取護衛、特注素材調達。**高額報酬金（Gold）**、良質装備。
  3. **民間・地域**（村長・市民・学者）: 害獣駆除、薬草採取、遺跡調査。若手の足慣らし・基礎経験値。
* **総合名声（Guild Fame）**:
  * 門を叩く**新規志願者のグレード（才能ランク・初期能力・稀少クラス）**や志願者数に直結。
* **勢力信頼度（Faction Trust）**:
  * 各勢力の依頼成否で増減。一定以上で「特命クエスト」「ランク昇格試験」「独占買取契約」などが解放。

### ⑤ パーティ編成とクラス（※次回検討・未確定）
* **検討中の候補案**:
  * 最大人数: 1〜4名編成（前衛・後衛等の配置）
  * 基本クラス候補: 戦士（物理攻防）、騎士（タンク）、盗賊（斥候/収集）、魔術師（火力）、僧侶（回復/生存）
  * 結果判定区分: 大成功 / 成功 / 失敗（負傷療養） / 大失敗（戦死ロスト）
* **ステータス**: 人数枠やクラス構成の詳細は次回作業時に確定予定。

### ⑥ 目標サイクルとゲームクリア
* **定期目標（月次監査）**: 30日ごとに冒険者連盟への上納金・実績報告（連続未達で認可取消ゲームオーバー）。
* **昇格試験とクリア**: 条件達成でギルドランク昇格試験が発生（F → E → D → C → B → A → Sランク）。Sランク到達でゲームクリア（エンディング）後、何世代にもわたるエンドレス経営モードへ移行。
* **周回引継ぎ（MetaState）**: 1プレイの `GameState` とは別に、全周回で蓄積されるアンロック情報（秘伝、施設技術等）を分離保持する。

---

## 5. 次回作業予定

1. **仕様の最終確定**:
   * **パーティ人数・編成仕様**（最大人数、ロール・クラス構成）の確定
2. **ドメインモデル・型定義のリファクタリング・新規作成**:
   * `src/core/adventurer.rs`: 年齢（age）、ステータス、指導員適性、状態（Active, Injured, Retired, Fallen）
   * `src/core/training.rs`（新規）: 訓練所（TrainingDojo）、教官スロット、成長率バフ計算
   * `src/core/quest.rs`: 発注勢力（ClientFaction）、パーティ編成、結果判定、報酬分配
   * `src/core/state.rs`: 総合名声（reputation/fame）、勢力信頼度マップ、1年365日カレンダー
   * `src/core/meta.rs`（新規）: 周回用メタデータ（`MetaState`）の雛形
3. **決定論的乱数（RNG）基盤の導入**:
   * `rand` / `rand_pcg` 等によるシード再現性のあるクエスト成否・志願者生成。
4. **Unitテストの拡充**:
   * パーティ派遣・結果判定、教官による成長率向上、加齢と引退、名声・勢力信頼度の変動テスト。





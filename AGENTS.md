# プロジェクト開発指針と合意事項 (AGENTS.md)

このドキュメントは、本プロジェクト（冒険者ギルド経営シミュレーション）において AI エージェントおよび開発者が遵守すべきアーキテクチャ設計・開発指針・合意事項を記録したものです。作業開始時には必ず本指針を確認してください。

---

## 1. 基本方針・合意事項（2026-09-16 確定）

### ① サーバー依存ゼロ（完全ローカル・オフライン完結）
* 外部サーバーやクラウドDBへの通信・依存は行わない。
* インフラ費用ゼロ、メンテナンスフリー、完全スタンドアロンで恒久的に動作するクライアント完結型アプリケーションとする。

### ② アーキテクチャ：`core/`（ゲームロジック）と `ui/`（描画）の完全分離
* **Rust の標準的プラクティスを徹底**:
  * `core` モジュール: ゲーム状態（`GameState`）、冒険者・クエスト・倉庫・収支などのドメインモデルおよび進行ロジック。**`egui` や `eframe` への依存を一切持たない純粋な Rust コード**とする。
  * `ui` モジュール: `eframe::App` のライフサイクルおよび `egui` による画面描画。`GameState` を受け取って表示し、ユーザー操作を `core` のメソッド呼び出しまたはイベントに変換するだけに徹する。
* **借用チェッカー対策**:
  * UI描画コンテキストとゲーム状態の可変借用が衝突しないよう、状態更新と描画の責務を明確に分ける。

### ③ セーブ機能とデータ保護（対改ざん性）
* **基盤**: `serde` / `serde_json` を全面導入し、状態構造体に `#[derive(Serialize, Deserialize)]` を付与。
* **開発フェーズ**:
  * デバッグ・検証の容易さを優先し、ローカルのプレーンな JSON 形式（例: `save_data.json`）で保存・読み込みを行う。
* **対改ざん性・将来対応**:
  * セーブ/ロード処理は抽象化（インターフェース化）しておく。
  * 将来的に改ざん防止を導入する際も、サーバーに頼らず **ローカル完結の暗号化（AES-256 等）または HMAC 署名付きバイナリ化** を差し込める設計とする。

### ④ テスト容易性（Unit テストの必須化）
* `core` ロジック（日次進行、固定費引き落とし、クエスト成否判定、アイテム売買、セーブ/ロード等）に対して、画面なし環境（DevContainer / CI）でも `cargo test` 一発で高速動作する Unit テストを必ず整備する。

---

## 2. 推奨ディレクトリ構成

```text
src/
├── main.rs              # エントリポイント、DevContainer/X11設定、フォント設定
├── app.rs               # GuildApp（eframe::App実装、タブ遷移、メインレイアウト）
├── core/                # 【GUI非依存】ゲームドメイン・ロジック・テスト
│   ├── mod.rs           # GameState の統括、save/load 実装
│   ├── state.rs         # ゲーム基本状態（Day, Gold, Rank, 等）
│   ├── adventurer.rs    # 冒険者データ・ステータス
│   ├── quest.rs         # クエストデータ・派遣・完了判定
│   ├── inventory.rs     # 倉庫アイテム・売却計算
│   └── finance.rs       # 固定費・人件費・収支台帳
└── ui/                  # 【egui依存】画面描画・コンポーネント
    ├── mod.rs
    ├── components/      # ヘッダー、固定フッター（日誌ログ）など
    └── views/           # 各タブの描画（ギルド情報、クエスト、冒険者、倉庫、職員、台帳）
```

---

## 3. 実装済みステータス（2026-09-17 完了）

* [x] **依存関係の追加**: `serde`, `serde_json` を導入。
* [x] **`core` モジュールの切り出し**:
  * `src/core/` 配下に `adventurer.rs`, `inventory.rs`, `quest.rs`, `finance.rs`, `state.rs`, `mod.rs` を抽出。
  * 完全 GUI 非依存のゲームロジックおよび JSON セーブ/ロード（`save_game`, `load_game`）を実装。
* [x] **Unit テストの整備**:
  * 日付進行・固定費引き落とし、アイテム売却、クエスト派遣/帰還、JSONセーブ/ロード往復の5件のテストを実装、すべてパス。
* [x] **`ui` モジュールおよび `app.rs` の整理**:
  * ヘッダー、フッター、6つの各タブ画面を `src/ui/` に完全分離。
  * `src/main.rs` はエントリポイント（50行程度）にスリム化。

---

## 4. 次回作業予定（要件確定・基礎設計フェーズ）

1. **ゲーム要件・仕様の確定（最優先）**:
   * 目標サイクル（定期審査／大型要請マイルストーン）とクリア後エンドレス経営のルール確定。
   * 冒険者のリスク（負傷療養 vs 死亡・引退）および雇用・新陳代謝サイクルの決定。
   * クエスト・パーティ編成仕様（人数・ロール）と勝敗判定ロジックの確定。
2. **ドメインモデル・基礎設計への落とし込み**:
   * 要件に沿った型定義（Rank, Status, ID体系, Event）へのリファクタリング。
   * 決定論的乱数（RNG）基盤の導入。
3. **ゲームループ実装・機能拡張**:
   * パーティ派遣・勝率・レベルアップ・相場変動の実装。

---

## 5. ゲーム要件・設計検討メモ（2026-09-18）

### ① ゲームサイクルと目的（合意済み方向性）
* **基本構造**: 単なるエンドレス経営は中だるみ・虚無になりやすいため、**「定期的な目標（マイルストーン）」をクリアしながら最終目標を目指し、クリア後はエンドレス経営で自由に遊べる**二段構えとする。
* **定期目標の候補案**:
  * **案A（経営ノルマ型）**: 冒険者連盟による定期ギルド監査（例: 30日ごとに資産・実績・ランク審査。連続未達で認可取消ゲームオーバー）。
  * **案B（イベント駆動型）**: 地域クライシス・大型要請（例: ○日後までに指定素材納入やボス討伐）。
* **最終到達点**: 最高位ギルドランク（Sランク）到達や公認ギルド昇格でクリア → その後エンドレスモードへ移行。

### ② 検討中の論点（次回検討予定）
1. **冒険者のリスク（失敗・損失の重さ）**:
   * マイルド（数日の負傷療養・病床離脱のみ）か、
   * ミドル（基本負傷＋危険依頼での引退・行方不明）か、
   * ハードコア（キャラロスト・死亡あり）か。
2. **冒険者の雇用・人材マネジメント**:
   * 初期メンバー固定育成か、
   * 酒場・窓口での定期的な新規冒険者の募集・採用・解雇・世代交代か。
3. **パーティ編成と勝率ロジック**:
   * 1パーティの最大人数（1〜4人）、前衛・後衛等のロール必要性。
   * 冒険者ステータス（攻撃・防御・素早さ・適性等）とクエスト難易度の照合。
4. **周回引継ぎ要素（New Game+ / メタ進行）**:
   * ゲームオーバー時やクリア時に、何らかの要素（施設技術、レシピ、秘伝書、名声ポイント、一部アイテムや冒険者の引継ぎ等）を持ち越せる仕組み。
   * **設計上の考慮点**: 1プレイの「`GameState`」とは別に、全周回で共有・蓄積される「`MetaState`（プレイヤー実績・アンロック情報）」の切り分けをデータ構造の基礎設計時から意識しておく。



